# Автомат обрабатывает нат. число N по алг.:
# 1. Строится двоичная запись числа N
# 2. К полученной записи дописываются разряды. Если число чётное, справа дописывается 10,
# если число нечетное - слева дописывается 1 и справа 01
# 3. Результат переводится в десятичную систему и выводится на экран
for i in range(1, 1000):
    x = bin(i)[2:]
    if i % 2 == 0:
        x = x + '10'
    else:
        x = '1' + x + '01'
    r = int(x, 2)
    if r > 516:
        print(i)
